1. Install Python
2. run: pip install pypdn watchdog pillow
3. Run: WatchTextures.bat

1. Google it

2. That's a single command that installs three separate Python packages at once. You can list as many packages as you want after pip install and it'll grab them all.

3. This runs the python script for the textures in this folder. It might assume your game is at C:\git\MacGame